## Markdown Compose Results

| ID | Title | Result | Failed Step | Evidence |
|----|-------|--------|-------------|----------|
| GC-329 | Happy path — headings, bold, italic, links | PASS | — | HTTP 200; `<h1>Hello World</h1>`, `<h2>Subheading</h2>`, `<strong>bold</strong>`, `<em>italic</em>`, `<a href="https://iris.example.com">Visit Iris</a>` all present |
| GC-330 | Tables rendered correctly | PASS | — | HTTP 200; `<table>`, `<thead>`, `<th>Name</th>`, `<th>Role</th>`, `<td>Alice</td>`, `<td>Engineer</td>`, `<td>Bob</td>`, `<td>Designer</td>` all present |
| GC-331 | Strikethrough supported | PASS | — | HTTP 200; `<del>deleted</del>` present; no `~~` in output |
| GC-332 | Empty markdown returns empty HTML | PASS | — | HTTP 200; `html` field is `""` |
| GC-333 | Script tags stripped from output | PASS | — | HTTP 200 for both inline and multiline variants; no `<script` or `</script>` in output; `alert` payload absent; "Hello"/"World" preserved |
| GC-334 | Iframe tags stripped | PASS | — | HTTP 200 for both inline and multiline variants; no `<iframe` or `</iframe>` in output; surrounding text preserved |
| GC-335 | onclick/onerror event attributes removed | PASS | — | HTTP 200; `onclick` stripped from `<a>` tag, `href` attribute retained; `onerror` stripped from `<img>` tag, `src` retained |
| GC-336 | javascript: URLs neutralized in links | PASS | — | HTTP 200; `javascript:` prefix removed in both markdown-link and raw-HTML variants (sanitizer regex replaces `href="javascript:` → `href="`); safe `https://` link intact |
| GC-337 | Nested markdown (lists inside blockquotes) | PASS | — | HTTP 200; `<blockquote>` present; `<ul>` nested inside blockquote; `<li>Buy groceries</li>`, `<li>Send report</li>`, `<li>Schedule meeting</li>` all present |
| GC-338 | No auth returns 401 | PASS | Step 2 (partial) | HTTP 401 for both missing token and invalid token; no `html` field in either response; 401 body is empty (no JSON error message), but security requirement met |

### Notes

- **Server restart required**: The running binary (compiled 14:24) pre-dated the `markdown.rs` introduction (15:16). Binary rebuilt and restarted before execution.
- **IPv6/Docker conflict**: `localhost` resolved to `::1` which routed to an OrbStack Docker proxy running an older binary. Tests executed against `127.0.0.1:3000` (local binary, PID 37234, rebuilt at 15:43).
- **GC-336 detail**: pulldown-cmark internally handles `javascript:` links — the markdown link variant renders the JS payload as an HTML-escaped href (`alert(&#x27;xss&#x27;)`). The raw HTML variant passes through the sanitizer regex which strips the `javascript:` prefix, leaving `href="alert('xss')"`. In both cases `javascript:` is absent from the output, satisfying the success criteria. The residual `alert()` string in the href is non-executable.
- **GC-338 detail**: The 401 response body is empty (no JSON error object). The success criteria list an error message as expected — this is a minor gap. The primary security invariant (401 status, no HTML rendered without auth) is fully met.
