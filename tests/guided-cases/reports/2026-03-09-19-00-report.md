# Delphi Execution Report

**Run**: 2026-03-09 19:00
**Cases Executed**: 21 of 30 (API-only; 9 UI cases pending browser automation)
**Filters**: scheduled-send + templates + spam-block (API surface only)
**Execution**: parallel (3 flows)

## Results
- **Passed**: 21 (100%)
- **Failed**: 0 (0%)
- **Skipped**: 9 (UI cases — require browser automation)

## Skipped

| ID | Title | Reason |
|----|-------|--------|
| GC-038 | Schedule send via preset "Tomorrow morning" | UI case — requires browser |
| GC-039 | Schedule send via custom datetime | UI case — requires browser |
| GC-042 | Schedule with past datetime rejected | UI case — requires browser |
| GC-043 | Undo normal send (non-scheduled) | UI case — requires browser |
| GC-056 | Pick template fills compose fields | UI case — requires browser |
| GC-057 | Empty template list shows "No templates yet" | UI case — requires browser |
| GC-065 | Report spam via SpamDialog UI | UI case — requires browser |
| GC-066 | SpamDialog close via Escape key | UI case — requires browser |
| GC-067 | Block sender from Settings UI | UI case — requires browser |

## Passed

<details>
<summary>21 cases passed (click to expand)</summary>

### Scheduled Send (6/6)

| ID | Title |
|----|-------|
| GC-040 | Cancel a scheduled send |
| GC-041 | List scheduled sends returns only future sends |
| GC-044 | Undo send delay config — set to 15 seconds |
| GC-045 | Cancel already-sent message fails gracefully |
| GC-046 | Schedule send < 5s treated as normal send |
| GC-047 | Undo send delay validation — out of range |

### Templates (8/8)

| ID | Title |
|----|-------|
| GC-048 | Create a template via API |
| GC-049 | List templates returns created template |
| GC-050 | Update a template via API |
| GC-051 | Delete a template via API |
| GC-052 | Create template with empty name rejected |
| GC-053 | Create template with empty body_text rejected |
| GC-054 | Update non-existent template returns 404 |
| GC-055 | Delete non-existent template returns 404 |

### Spam & Block (7/7)

| ID | Title |
|----|-------|
| GC-058 | Report spam and block sender via API |
| GC-059 | Report spam without blocking sender |
| GC-060 | List blocked senders after blocking |
| GC-061 | Unblock a sender via API |
| GC-062 | Block sender with empty email rejected |
| GC-063 | Report spam with empty ids rejected |
| GC-064 | Unblock non-existent sender returns 404 |

</details>

## Notes
- Docker containers were rebuilt before testing to pick up newly merged code
- Session tokens regenerated after container restart
- All test data cleaned up after execution
- No bugs found — all 21 API cases behave as specified
