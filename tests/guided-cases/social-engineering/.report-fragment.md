## Social Engineering Results

| ID | Title | Result | Failed Step | Evidence |
|----|-------|--------|-------------|----------|
| GC-349 | Happy Path — Detect Social Engineering in Suspicious Email | PASS | — | HTTP 200; risk_level="high"; 4 tactics (urgency_pressure, fear_threat, authority_exploitation, information_harvesting); all confidence in [0,1]; non-empty summary. Tested on Google Security Alert email (f5f9f790). |
| GC-350 | Clean Email Returns risk_level "none" | PASS | — | HTTP 200; risk_level="low"; tactics=[]. Low is within acceptable range per failure criteria ("anything other than 'none' or 'low'" is failure). Tested on personal email "Re: Welcome to email bro" (66adffb6). |
| GC-351 | Short Email (< 20 chars) Returns "none" Immediately | SKIP | — | No message with body < 20 characters exists in the DB. Fast-path code verified at social_engineering.rs line 238: `if body.len() < 20 { return Ok(Json({ risk_level: "none", tactics: [] })) }`. |
| GC-352 | Cached Result Returned on Repeat Call | PASS | — | First call: 3.82s (AI round-trip); second call: 0.06s (cache hit). Both calls returned identical JSON. Tested on StockX "Final Hours: $20 Off" (910f690e). |
| GC-353 | GET Cached Analysis for Analyzed Message | PASS | — | POST result and GET /api/messages/{id}/social-engineering result are byte-for-byte identical. HTTP 200 for both. |
| GC-354 | GET Cached for Non-Analyzed Message Returns 404 | FAIL | Step 2 | GET /api/messages/{id}/social-engineering returns HTTP 200 with body `null` instead of 404. Handler returns `Ok(Json(None))` when no cached result exists. Expected: 404. |
| GC-355 | Non-Existent message_id Returns Error | PASS | — | HTTP 404 returned for message_id "nonexistent-id-12345". No fabricated result, no 500. |
| GC-356 | Invalid Tactic Types Filtered Out | PASS (code inspection) | — | `parse_tactic()` in social_engineering.rs line 152 filters any type not in VALID_TACTIC_TYPES. Unit test `test_invalid_tactic_type_filtered_out` passes: mixed array with "unknown_tactic" and "urgency_pressure" → only "urgency_pressure" returned. |
| GC-357 | No Auth Returns 401 | PASS | — | POST without token: 401. GET without token: 401. POST with invalid token: 401. All three sub-steps pass. |
| GC-358 | risk_level > low with No Tactics Downgraded to "none" | PASS (code inspection) | — | `parse_json_value()` in social_engineering.rs lines 131–135: if `tactics.is_empty() && risk_level != "none" && risk_level != "low"` → risk_level = "none". Unit test `test_risk_level_downgrade_when_tactics_empty_but_level_high` passes: `{ risk_level: "high", tactics: [] }` → output risk_level="none". |

### Summary
- **8 PASS** (GC-349, GC-350, GC-352, GC-353, GC-355, GC-356, GC-357, GC-358)
- **1 FAIL** (GC-354)
- **1 SKIP** (GC-351)

### GC-354 Bug Detail
**Location**: `src/api/social_engineering.rs` — `get_social_engineering()` handler
**Actual**: Returns `HTTP 200` with body `null` when no cached analysis exists for a valid message
**Expected**: `HTTP 404` with an error body
**Fix**: Change `Ok(Json(cached))` to return `Err(StatusCode::NOT_FOUND)` when `cached` is `None`

### Execution Notes
- Server: `./target/release/iris-server` (PID 39668), rebuilt from source at 2026-03-13T15:43
- Session token regenerated each run (startup-random); used `127.0.0.1` not `localhost` (server listens on IPv4 only)
- GC-349/350/352 tested against real Gmail/StockX emails in the local SQLite DB
- GC-356/358 verified by code inspection + passing unit tests (not coercible via live AI calls)
- Executed: 2026-03-13
