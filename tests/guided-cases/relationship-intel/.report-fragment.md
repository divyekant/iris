## relationship-intel Results

| ID | Title | Result | Failed Step | Notes |
|----|-------|--------|-------------|-------|
| GC-389 | Happy path — intelligence summary returns top contacts with scores | failed | Step 2–3 | Summary endpoint returns `score` (not `relationship_score`) and `last_contact` as Unix epoch integer (not ISO 8601 string). Ordering, count, and value range all correct. Two field-naming/type mismatches vs spec. |
| GC-390 | Happy path — contact intelligence detail returns all fields | failed | Step 5 | Response uses `communication_pattern` (singular) vs spec's `communication_patterns` (plural). All data present and values valid: relationship_score=0.676, score_breakdown with 5 components, stats (sent+received=total), common_topics array, most_active_hour=2 in [0,23]. Functional pass, schema naming mismatch. |
| GC-391 | AI summary — narrative summary generated for a contact | failed | Step 2 | AI provider is configured and returns 200 with coherent narrative (365 chars) and 3 key insights. Missing `email` field in response (spec expects `email` matching requested contact). summary and key_insights content are correct. |
| GC-392 | Summary with custom limit parameter — correct number returned | passed | N/A | limit=3 returns 3 entries, limit=1 returns 1 (top contact itsme.div@gmail.com), default returns 10, top-3 ordering stable across all limit values. |
| GC-393 | Negative — intelligence detail for non-existent contact | failed | Step 3 | Returns 404 (not 500, correct). However body is empty (content-length: 0); spec requires error message present in 404 body. No data leakage, no crash. |
| GC-394 | Negative — AI summary for non-existent contact returns error | passed | N/A | Returns 404 with empty body. No hallucinated data, no AI provider error leaked, no 500. Consistent with GC-393 behavior (empty 404 body is acceptable given spec allows 404). |
| GC-395 | Negative — missing email param in path returns 404 or 405 | failed | Step 1 | `GET /api/contacts//intelligence` returns 200 with data instead of 4xx. Empty email string matches a query that returns stats (total_emails=13, sent_by_you=13 — likely aggregating all sent messages). Steps 2 and 3 correctly return 404. |
| GC-396 | Edge — URL-encoded email address in path decoded correctly | passed | N/A | `noreply%40stockx.com` decoded to `noreply@stockx.com` in response (verified). `%40` not left encoded. Non-existent `user%2Btag%40example.com` returns 404 (not 400 or 500). AI summary endpoint also handles encoded path correctly (404 for unknown contact). No double-encoding artifacts. |
| GC-397 | Edge — contact with zero sent emails shows correct one-directional stats | passed | N/A | noreply@stockx.com: sent_by_you=0, received=83, total_emails=83 (total==received). avg_response_time_hours=0.0. relationship_score=0.49 < 0.676 (bidirectional contact itsme.div@gmail.com). All directional stats correct. |
| GC-398 | Security — SQL injection attempt in email path parameter rejected safely | passed | N/A | OR-1=1 injection: 404. UNION SELECT: 404. DROP TABLE: 404. DB integrity confirmed (summary returns 200 with intact data after all attempts). AI summary endpoint also returns 404 for injected path. No 500, no schema leaked, relationship_scores table intact. |

### Summary
- **Passed**: 5 (GC-392, GC-394, GC-396, GC-397, GC-398)
- **Failed**: 5 (GC-389, GC-390, GC-391, GC-393, GC-395)
- **Skipped**: 0

### Key Issues Found

1. **GC-389/GC-390: Field naming mismatches** — Summary endpoint returns `score` instead of `relationship_score`; detail endpoint returns `communication_pattern` (singular) instead of `communication_patterns` (plural); `last_contact` in summary is Unix epoch integer not ISO 8601 string.

2. **GC-391: Missing `email` field in AI summary response** — The `/intelligence/ai-summary` endpoint returns only `{summary, key_insights}` without the `email` field the spec requires.

3. **GC-393/GC-394: Empty 404 body** — Both non-existent contact endpoints return 404 with `content-length: 0` instead of a JSON error body with a message field. GC-394 passes because the spec allows 404 without explicit error body requirement for the non-AI case; GC-393 fails because the spec explicitly requires "error message is present" on 404.

4. **GC-395: Empty email path segment returns 200** — `GET /api/contacts//intelligence` returns 200 with data (empty string email matches some query returning aggregate stats). Should return 4xx. Bug in input validation — empty path segment must be rejected.

### Execution Notes
- Session token in task instructions was expired (server had been restarted). Used `/api/auth/bootstrap` to obtain current token `afbf9aa5...`.
- AI provider was configured and healthy (Anthropic); AI summary calls succeeded.
- Test contacts used: `itsme.div@gmail.com` (bidirectional, 5 emails), `noreply@stockx.com` (receive-only, 83 emails), `nobody@does-not-exist.test` (non-existent).
