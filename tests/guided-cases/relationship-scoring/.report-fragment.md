## relationship-scoring Results

| ID | Title | Result | Failed Step | Notes |
|----|-------|--------|-------------|-------|
| GC-419 | Compute Relationships — Happy Path Returns Category Counts | passed | N/A | POST /api/contacts/relationships/compute returned 200. computed=12, strong=1, regular=3, weak=8, dormant=0. Sum matches computed. All fields non-negative. |
| GC-420 | List Relationships — Paginated Response | passed | N/A | GET with limit=2&offset=0 returned 2 entries (itsme.div@gmail.com score=0.915, mailer-daemon score=0.588). Second page with offset=2 returned 2 different entries. No duplicates across pages. All entries have required fields. Scores in [0,1] and descending order confirmed. |
| GC-421 | Get Specific Contact Relationship Detail with Score Breakdown | passed | N/A | GET /api/contacts/relationships/itsme.div%40gmail.com returned 200. All 5 factor scores present and in [0,1]. Weighted sum check: expected=0.9147, got=0.915 (diff=0.0003, within 0.01 tolerance). strength_label=strong matches overall_score=0.915 (>=0.7). total_sent=3, total_received=2, avg_response_time_secs=1300. last_sent and last_received both present as Unix timestamps. |
| GC-422 | Get Relationship Stats — Aggregated Metrics | passed | N/A | GET /api/contacts/relationships/stats returned 200. by_strength sum (1+3+8+0=12) equals total_contacts=12. by_strength matches compute response exactly. most_active={email: "itsme.div@gmail.com", score: 0.915} present and valid. avg_score=0.4. |
| GC-423 | List Relationships Filtered by Strength | passed | N/A | strength=strong filter returned exactly 1 contact (itsme.div@gmail.com, score=0.915 >=0.7). strength=weak filter returned 8 contacts, all with strength_label=weak and scores in [0.15, 0.4). All strong contacts appear in unfiltered list. Empty dormant filter returned 200 with empty array. |
| GC-424 | Get Relationship for Non-Existent Contact Returns 404 | passed | N/A | GET /api/contacts/relationships/ghost%40nonexistent-domain-iris-test.com returned 404. No internal details (SQL, stack traces, column names) in empty response body. Subsequent stats request returned 200 — server fully stable. |
| GC-425 | List Relationships with Invalid Sort Parameter | passed | N/A | sort=hacker returned 200 with safe default (overall_score DESC) — acceptable per spec. sort=score%3B%20DROP%20TABLE%20contacts-- returned 200 with safe default (allowlist approach prevents injection). sort= (empty) returned 200 with safe default. Server stable after all invalid inputs. contacts table intact (12 contacts confirmed). Note: implementation uses allowlist match in Rust match arm; unrecognized values fall to _ => "overall_score DESC". |
| GC-426 | Compute Relationships with No Messages in Database | skipped | N/A | Precondition not met — live DB has 48 messages. Empty-DB state cannot be reproduced without clearing test data. Partial validation: dormant filter (0 contacts) returns 200 with empty array {"contacts":[],"total":0}, confirming zero-result graceful behavior. Compute on non-empty DB returns 200. |
| GC-427 | URL-Encoded Email in Relationship Path Parameter | passed | N/A | itsme.div%40gmail.com (%40 = @) returned 200 and resolved correctly to itsme.div@gmail.com. Raw itsme.div@gmail.com also returned 200 with identical data. alice%2Bwork%40example.com (no such contact) returned 404 (not 500). Server stable. Axum Path extractor correctly percent-decodes path parameters. |
| GC-428 | SQL Injection in Email Path Parameter | passed | N/A | OR-injection (' OR '1'='1) returned 400. UNION injection returned 404. Stacked DROP TABLE injection returned 404. No relationship data exposed via any payload. No SQL/panic details in response bodies. Stats after injections: total_contacts=12 (data intact). rusqlite parameterized queries prevent injection; single-statement execution prevents stacked queries. |
