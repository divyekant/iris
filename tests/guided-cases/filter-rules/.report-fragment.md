## Filter Rules Results

**Executed**: 2026-03-09
**Server**: http://127.0.0.1:3000
**Total**: 10 cases | **Passed**: 10 | **Failed**: 0

| ID | Title | Result | Failed Step | Evidence |
|----|-------|--------|-------------|----------|
| GC-109 | Create filter rule happy path | passed | N/A | POST 201, id=d7544f47, name/conditions/actions/is_active all correct |
| GC-110 | List filter rules | passed | N/A | Both POSTs 201, GET 200, array has 3 entries, both names found, all fields present |
| GC-111 | Update filter rule | passed | N/A | POST 201, PUT 200, name updated to "Updated rule name", is_active=false, conditions/actions preserved |
| GC-112 | Delete filter rule | passed | N/A | POST 201, first DELETE 204, second DELETE 404, rule absent from GET list |
| GC-113 | Create with empty name | passed | N/A | POST 400, no empty-name rule in list (note: response body empty, no error message text) |
| GC-114 | Create with empty conditions | passed | N/A | POST 400, "No conditions rule" absent from list |
| GC-115 | Create with empty actions | passed | N/A | POST 400, "No actions rule" absent from list |
| GC-116 | Update nonexistent rule | passed | N/A | PUT 404, "Ghost rule" absent from list, no side effects |
| GC-117 | Create multiple conditions and actions | passed | N/A | POST 201, 3 conditions (from/subject/has_attachments) and 3 actions (mark_read/label/archive) all correct, label value="promotions", confirmed via GET list |
| GC-118 | Toggle rule active/inactive | passed | N/A | POST 201 is_active=true, PUT deactivate 200 is_active=false (confirmed in GET), PUT reactivate 200 is_active=true (confirmed in GET), other fields unchanged |

### Observations

- **Validation responses lack error messages**: GC-113, GC-114, GC-115 all correctly return HTTP 400 but with empty response bodies. The test cases expect an error message indicating what's wrong (e.g., "name is required"). This is a minor UX gap -- the API rejects invalid input but doesn't tell the caller why.
- **Actions include nullable `value` field**: All action objects include `"value": null` even when not applicable (e.g., archive, mark_read). This is fine for consumers but worth noting.
- **`account_id` is null**: All created rules have `account_id: null`, suggesting filter rules are currently global rather than per-account.
